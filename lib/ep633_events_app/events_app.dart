import 'package:flutter/material.dart';

import 'ui/events_main_page.dart';

class EventsApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: EventsMainPage(),
    );
  }
}

