import 'package:flutter/material.dart';

import 'date_app_home_page.dart';
import 'date_profile_page.dart';

class DateProfileApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: DateAppHomePage(),
    );
  }
}
