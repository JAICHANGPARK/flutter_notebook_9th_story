import 'package:flutter/material.dart';

import 'new_once_login_page.dart';

class NewOnceApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: NewOnceLoginPage(),
    );
  }
}
